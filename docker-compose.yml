services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: service-auditor
    restart: unless-stopped
    ports:
      - "8080:80"
    volumes:
      - ./:/var/www/html
    environment:
      - APACHE_RUN_USER=www-data
      - APACHE_RUN_GROUP=www-data
